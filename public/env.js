// Runtime API base override for obra_guard front-end.
// Change this value to point to your backend URL (protocol + host + port).
// Example for local dev: http://localhost:80
// Example for prod: https://api.tu-dominio.com
window.AF_API_URL = window.AF_API_URL || 'http://localhost:80';
